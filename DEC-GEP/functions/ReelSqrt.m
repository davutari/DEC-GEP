% ReelLog(x) computes the square root of the absolute value 
% of the input x to avoid negative value x.
% It ensures real value results from sqrt(x).
function [result]=ReelSqrt(x)
     result=sqrt(abs(x));
end


